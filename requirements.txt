# Union of every package required by the four pipeline modules.
# Mirrors _MASTER_REQUIRED in master.py, which auto-installs these at import
# time if they are missing (Colab-friendly).  verify_docs.py check 7 holds the
# two lists to each other.
#
# `spacecost` holds Stage 3's reference tables -- 36 launch vehicles, 41
# propellants, the delta-v segments, the operational costs and the storage
# systems -- which used to live in modules/transportation.py.  It is not on
# PyPI yet, so it is pinned to a TAGGED git ref; an untagged URL would change
# what a fresh install gets without anything here moving.
#
# `asteroid_catalog` holds Stage 1's builder -- the four survey fetchers, the
# cross-match, the H-derivation, the validator and the Bus-DeMeo composition
# table -- which used to live in modules/catalog.py.  Same arrangement and same
# reason: it is not on PyPI, so it is pinned to a TAGGED git ref.
requests
pandas
numpy
yfinance
tqdm
pyarrow
spacecost @ git+https://github.com/loggger101/spacecost@v0.3.2
asteroid-catalog @ git+https://github.com/loggger101/AsteroidCatalog@v0.2.0
